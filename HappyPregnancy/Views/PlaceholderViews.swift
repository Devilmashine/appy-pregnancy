import SwiftUI

struct NutritionView: View {
    var body: some View {
        Text("Nutrition View (to be implemented)")
            .navigationTitle("Питание")
    }
}

struct ExercisesView: View {
    var body: some View {
        Text("Exercises View (to be implemented)")
            .navigationTitle("Упражнения")
    }
}

struct SleepView: View {
    var body: some View {
        Text("Sleep View (to be implemented)")
            .navigationTitle("Сон")
    }
}

struct BirthPreparationView: View {
    var body: some View {
        Text("Birth Preparation View (to be implemented)")
            .navigationTitle("Подготовка к родам")
    }
}

struct BabyView: View {
    var body: some View {
        Text("Baby View (to be implemented)")
            .navigationTitle("Малыш")
    }
}

struct CalculatorsView: View {
    var body: some View {
        Text("Calculators View (to be implemented)")
            .navigationTitle("Калькуляторы")
    }
}

struct MedicalTestsView: View {
    var body: some View {
        Text("Medical Tests View (to be implemented)")
            .navigationTitle("Анализы")
    }
}
